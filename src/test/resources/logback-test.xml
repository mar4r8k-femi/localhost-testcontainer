<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Keep Testcontainers + AWS SDK noise at WARN during tests -->
  <logger name="org.testcontainers"          level="WARN"/>
  <logger name="com.github.dockerjava"       level="WARN"/>
  <logger name="software.amazon.awssdk"      level="WARN"/>

  <!-- Suppress Ryuk retry warnings on macOS Docker Desktop: Ryuk's socket
       takes a moment to bind after the container starts; the retries always
       succeed and the warnings are purely cosmetic. -->
  <logger name="org.testcontainers.utility.RyukResourceReaper" level="ERROR"/>

  <!-- Your own code at DEBUG -->
  <logger name="com.example"                 level="DEBUG"/>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>